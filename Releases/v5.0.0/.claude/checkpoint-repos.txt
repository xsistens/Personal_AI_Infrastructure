# CheckpointPerISC opt-in allowlist
#
# One absolute repo path per line. Lines starting with `#` and blank lines are ignored.
# Both `~/...` and `$HOME/...` are expanded by the hook.
#
# On every ISC `[ ]`->`[x]` transition, CheckpointPerISC.hook.ts checks each repo
# below for uncommitted changes; if any, it auto-commits with subject:
#   ISC-{N} ({slug}): {description}
#
# Default = ~/.claude only. Adding an active-WIP repo will start auto-committing
# your in-flight work alongside ISA progress (Hankweave-style whole-repo snapshots).
# Inspection: bun ~/.claude/PAI/TOOLS/Checkpoint.ts list <slug>
# Rollback (preview only): bun ~/.claude/PAI/TOOLS/Checkpoint.ts rollback <slug> <isc-id>

~/.claude
